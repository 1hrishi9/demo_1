pipeline {
    agent {
        node {
            label 'lable1'
        }

    }
    stages {
        stage('git commit') {
            steps {
              sh '''
         git clone https://github.com/1hrishi9/studentapp-ui.git
        
        '''  
            }
        }
        stage('build') {
            steps {
                sh '''
               apt install maven
               cd /home/ubuntu/workspace/studentapp-ui/
               mvn clean
               mvn package
                '''
            }
        }
        stage('test') {
            steps {
                echo "successfully test"
            }
        }
        stage('deploy') {
            steps {
              echo "successfully deployed"
            }
        }
        
        }
    }
}
